[build-system]
requires = ["hatchling==1.32.0"]
build-backend = "hatchling.build"

[project]
name = "ceratops-deployment-fixture"
version = "1.0.0"
description = "Harmless deployment contract fixture"
requires-python = ">=3.13,<3.14"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/ceratops_deployment_fixture"]
