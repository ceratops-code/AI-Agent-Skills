interface:
  display_name: "Ceratops GH Ship Change"
  short_description: "Ship repo changes through GitHub with live checks"
  default_prompt: "Use $ceratops-gh-ship-change to ship these local changes through GitHub with the bundled live checks, publish any relevant artifacts, verify them locally, and clean up state."
